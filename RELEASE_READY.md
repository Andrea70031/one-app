# ONE 1.0 release readiness

This branch is the final pre-publication candidate.

Automated checks must pass before merge:

- dependency install / lockfile sync
- TypeScript strict check
- Expo Doctor

After merge, the next operational phase is App Store publication/build signing.
